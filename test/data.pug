html(lang='en')
  head
    title Pug
    script(type='text/javascript').
      const foo = true;
      let bar = function() {};
      if (foo) {
        bar(1 + 5)
      }
  body
    h1('(click)'='angulay()') Pug - node template engine
    .test
      | asd
      | sdasd
      span asd
    #container.col
      p You are amazing
      p
        | Pug is a terse and simple
        | templating language with a
        | strong focus on performance
        | and powerful features.
    div
      img(src='{{itemImageUrl}}')
    p {{title}}