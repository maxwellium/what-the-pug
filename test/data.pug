doctype html
html(lang='en')
  head
    title Pug
    script(type='text/javascript').
      const foo = true;
      let bar = function() {};
      if (foo) {
        bar(1 + 5)
      }
  body
    h1(
      (click)='doSth()',
      [class.test]='beSth'
    ) Pug - node template engine
    a([routerLink]='link') test with one attribute
    a(
      [routerLink]='link',
      [attr.title]='desc'
    ) test with two attributes
    .test
      | asd
      | sdasd
      span asd
    #container.col
      p You are amazing
      p
        | Pug is a terse and simple
        | templating language with a
        | strong focus on performance
        | and powerful features.
    div
      img(src='{{itemImageUrl}}')
    p {{title}}